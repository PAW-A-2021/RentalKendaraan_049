@model IEnumerable<RentalKendaraan.Models.KondisiKendaraan>

@{
    ViewData["Title"] = "Index";
}

<h1>Kodisi Kendaraan</h1>

<p>
    <a asp-action="Create">Tambah Data</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayName("Kondisi")
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NamaKondisi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdKondisi"><i class="fa fa-marker fa-md"></i></a> |
                <a asp-action="Details" asp-route-id="@item.IdKondisi"><i class="fa fa-list"></i></a> |
                <a asp-action="Delete" asp-route-id="@item.IdKondisi"><i class="fa fa-trash-alt fa-md"></i></a>
            </td>
        </tr>
}
    </tbody>
</table>
